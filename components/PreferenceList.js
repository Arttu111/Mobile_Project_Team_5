import React from 'react';
import { View, Text} from 'react-native';

const PreferencesList = () => {
  //load books based on user preferences in this component

  return (
      <View >
        <Text>Preferences List</Text>
      </View>
  );
};

export default PreferencesList;
